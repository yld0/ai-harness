AUTH_SECRETPHRASE=
SECRET_KEY=test
# Import-time `agent_config` snapshot must see echo when using `--env-file testing.env`, or
# the suite will try ProviderRouter without keys and raise. Local/dev keys may also be
# set here; `DEV_ECHO_MODE=echo` keeps pytest offline-first.
DEV_ECHO_MODE=true